server {
	listen 80;

	location / {
		default_type text/html;
		
		content_by_lua '
			os.execute("sh /opt/lua/lua.sh")

			local file = "/var/www/lua/output.html"
			local f = io.open(file, "rb")
			local content = f:read("*all")
			f:close()

			ngx.print(content)
	
		';
	}
}

